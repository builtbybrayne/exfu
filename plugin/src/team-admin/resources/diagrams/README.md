# Team-admin diagrams

PNGs that ship only in the team-admin plugin. Drop the generated PNG here when you produce it from the brief in `plugin/planning/diagram-instructions/<slug>.md`.

Pending:

- `admin-vs-user.png` (TODO) — admin plane vs user domain. Brief: `plugin/planning/diagram-instructions/admin-vs-user-domain.md`.
- `seniority-trust.png` (TODO) — seniority and trust roles across organisational levels. Brief: `plugin/planning/diagram-instructions/seniority-trust-roles.md`.

Path at runtime: `${CLAUDE_PLUGIN_ROOT}/resources/diagrams/<slug>.png`.
