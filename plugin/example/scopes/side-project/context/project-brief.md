# Recipe engine -- project brief

A weekend experiment: a small recommendation engine that suggests recipes from
what's already in the fridge. Stack is Python plus a local embedding model.
Success looks like a working prototype that my partner actually uses twice in
one week. No deadlines, no stakeholders, fun first.
