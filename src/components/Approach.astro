<section id="approach" class="approach">
  <div class="approach-inner section-container">
    <div class="approach-header reveal">
      <h2>This isn't just a tech&nbsp;setup.</h2>
      <p class="approach-lead">I listen. Then I build around how you actually&nbsp;work.</p>
    </div>

    <div class="approach-columns">
      <div class="approach-col reveal">
        <h3 class="approach-col-label">Psychotherapist + technologist</h3>
        <p>I'm trained in both. I listen, I adapt to you, and I'm patient. Whatever comes up in the process, I'm comfortable working through it with&nbsp;you.</p>
      </div>
      <div class="approach-col reveal">
        <h3 class="approach-col-label">Neurodiversity friendly</h3>
        <p>Executive function challenges are literally what ExFu is named after. If you have ADHD, or suspect you might, these sessions are designed with you in&nbsp;mind.</p>
      </div>
    </div>
  </div>
</section>

<style>
  .approach {
    background: var(--color-surface-light);
    padding-block: var(--section-padding);
  }

  .approach-header h2 {
    font-size: var(--text-display);
    color: var(--color-primary);
    margin-bottom: var(--space-lg);
  }

  .approach-lead {
    font-family: var(--font-display);
    font-size: var(--text-h3);
    font-weight: 400;
    line-height: 1.4;
    color: var(--color-primary);
  }

  .approach-columns {
    display: flex;
    flex-direction: column;
    gap: var(--space-xl);
    margin-top: var(--space-2xl);
  }

  .approach-col-label {
    font-family: var(--font-body);
    font-size: var(--text-small);
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--color-accent);
    margin-bottom: var(--space-sm);
  }

  .approach-col p {
    font-size: var(--text-body-lg);
    line-height: 1.7;
    color: var(--color-text-light);
  }

  @media (min-width: 768px) {
    .approach-columns {
      flex-direction: row;
      gap: var(--space-3xl);
    }

    .approach-col {
      flex: 1;
    }
  }
</style>
