<section id="recognition" class="recognition">
  <div class="recognition-inner section-container">
    <div class="recognition-stack">
      <p class="recognition-line reveal">You've got projects.</p>
      <p class="recognition-line reveal">You've got ideas.</p>
      <p class="recognition-line reveal">You've got workflows and systems, but no perfect tool.</p>
      <p class="recognition-context reveal">
        Maybe you're using ChatGPT. Maybe you've moved to Claude. But you
        haven't crossed from "helpful chats" to "something you'd hand real work&nbsp;to."
      </p>
      <p class="recognition-line recognition-standout reveal">You need a second brain that understands your workflow and actually does stuff for&nbsp;you.</p>
      <p class="recognition-kicker reveal">
        One afternoon sets you up with your essential second brain, and the confidence to improve it&nbsp;yourself.
      </p>
    </div>
  </div>
</section>

<style>
  .recognition {
    background: var(--color-surface-light);
    padding-block: var(--section-padding);
  }

  .recognition-stack {
    max-width: var(--content-width);
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
  }

  .recognition-line {
    font-family: var(--font-display);
    font-size: var(--text-h3);
    font-weight: 400;
    line-height: 1.3;
    color: var(--color-primary);
  }

  .recognition-standout {
    font-size: var(--text-h2);
    font-style: italic;
    padding-top: var(--space-xs);
  }

  .recognition-context {
    font-size: var(--text-body-lg);
    line-height: 1.7;
    color: var(--color-text-light);
    padding-top: var(--space-sm);
  }

  .recognition-kicker {
    font-family: var(--font-display);
    font-size: var(--text-h2);
    font-weight: 400;
    line-height: 1.25;
    color: var(--color-accent);
    padding-top: var(--space-xs);
  }
</style>
